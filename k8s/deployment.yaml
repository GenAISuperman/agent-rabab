apiVersion: apps/v1
kind: Deployment
metadata:
  name: "rabab"
  namespace: "agents-dev"
  labels:
    app: "rabab"
    team: "dev"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "rabab"
  template:
    metadata:
      labels:
        app: "rabab"
        team: "dev"
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: "sa-rabab"
      containers:
        - name: "rabab"
          image: "acrshoppoc.azurecr.io/agents/rabab:latest"
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: "config-rabab"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
